# 📚 Library Management System (Python)

A simple **terminal-based Library Management System** written in Python.  
It allows you to **add books**, **register users**, **borrow/return books**, **search books**, and **save/load data** using a JSON file.

---

## ✅ Features

- 📖 Add new books (available or already borrowed)
- 🧑 Register new users (optionally with borrowed books)
- 📚 Borrow books (by user name or user ID)
- 🔁 Return books (by user name or user ID)
- 🔍 Search books (by title or book ID)
- 💾 Save all data into a JSON file (`library_data.json`)
- 📂 Load saved data from the JSON file

---

## 🛠️ Requirements

- Python 3.8+ (recommended)
- No external libraries needed (only uses built-in `json`)

---

## ▶️ How to Run

1. Save the code in a file, for example:
   ```bash
   library_system.py
